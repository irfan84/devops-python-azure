name: terraform-ci-$(Date:yyyyMMdd)$(Rev:.r)

trigger:
  batch: true
  branches:
    include:
      - main
  paths:
    include:
      - terraform/**
      - pipelines/terraform-ci.yml
      - pipelines/terraform-cd.yml
    exclude:
      - app/**
      - tests/**

pr:
  branches:
    include:
      - main
  paths:
    include:
      - terraform/**

variables:
  - group: rg-tfstate-devopspy-dev
  - name: TF_ROOT
    value: terraform/main
  - name: TF_VERSION
    value: "1.6.6"
  - name: CHECKOV_VERSION
    value: "3.2.0"

stages:
- stage: ValidateAndPlan
  displayName: "Terraform Validate + Plan + IaC Scan"
  jobs:
  - job: Plan
    displayName: "Plan"
    pool:
      name: self-hosted-dev

    steps:
    - checkout: self
      clean: true

    - task: TerraformInstaller@1
      displayName: "Install Terraform $(TF_VERSION)"
      inputs:
        terraformVersion: "$(TF_VERSION)"

    - task: PowerShell@2
      displayName: "IaC Security Scan (Checkov)"
      inputs:
        targetType: inline
        script: |
          $ErrorActionPreference = "Stop"
          python -m pip install --upgrade pip
          pip install "checkov==$(CHECKOV_VERSION)"
          checkov -d "$(TF_ROOT)" --quiet

    - task: AzureCLI@2
      displayName: "OIDC Login + Dual-IP Bridge + Plan"
      inputs:
        azureSubscription: "sc-devopspy-terraform-oidc"
        scriptType: ps
        scriptLocation: inlineScript
        addSpnToEnvironment: true
        inlineScript: |
          $ErrorActionPreference = "Stop"

          $baseDir   = "$(Build.SourcesDirectory)"
          $tfDir     = Join-Path $baseDir "$(TF_ROOT)"
          $planPath  = Join-Path $tfDir "tfplan"

          Push-Location $tfDir
          $currentAgentIp = $null

          try {
              # 1. Dual-IP Bridge Injection
              # Get Dynamic Live Runner IP
              $currentAgentIp = (Invoke-RestMethod -Uri "https://ifconfig.me/ip").Trim()
              Write-Host "==> Bridging Live Runner IP: $currentAgentIp"
              az keyvault network-rule add --name "$env:KV_NAME" --ip-address "$currentAgentIp"

              # Add Secondary Agent IP (from Library)
              if (-not [string]::IsNullOrWhiteSpace($env:TF_VAR_AgentIP)) {
                  Write-Host "==> Bridging Secondary Agent IP (TF_VAR_AgentIP): $env:TF_VAR_AgentIP"
                  az keyvault network-rule add --name "$env:KV_NAME" --ip-address "$env:TF_VAR_AgentIP"
              }

              Write-Host "==> Waiting 45s for Azure Network Propagation..."
              Start-Sleep -Seconds 45

              # 2. OIDC Authentication Setup
              Write-Host "==> OIDC Authentication Setup"
              $env:ARM_CLIENT_ID       = $env:servicePrincipalId
              $env:ARM_TENANT_ID       = $env:tenantId
              $env:ARM_OIDC_TOKEN      = $env:idToken
              $env:ARM_SUBSCRIPTION_ID = $env:TF_SUBSCRIPTION_ID
              $env:ARM_USE_OIDC        = "true"

              # 3. Reset Local State
              Write-Host "==> Resetting local Terraform state"
              $dotTerraform = Join-Path $tfDir ".terraform"
              if (Test-Path $dotTerraform) {
                  Remove-Item -Path $dotTerraform -Recurse -Force -ErrorAction SilentlyContinue
              }

              # 4. Terraform Workflow
              Write-Host "==> Terraform init"
              terraform init -reconfigure -input=false -force-copy `
                -backend-config="resource_group_name=$env:TF_STATE_RG" `
                -backend-config="storage_account_name=$env:TF_STATE_SA" `
                -backend-config="container_name=$env:TF_STATE_CONTAINER" `
                -backend-config="key=$env:TF_STATE_KEY" `
                -backend-config="use_oidc=true" `
                -backend-config="use_azuread_auth=true"

              if (!(Test-Path ".terraform.lock.hcl")) { throw "CRITICAL: .terraform.lock.hcl not found after init!" }

              Write-Host "==> Terraform fmt check"
              terraform fmt -check -recursive

              Write-Host "==> Terraform validate"
              terraform validate

              Write-Host "==> Terraform plan"
              terraform plan -out="$planPath" -input=false

              if (!(Test-Path "$planPath")) { throw "CRITICAL: tfplan file not found!" }

              Write-Host "==> Terraform show"
              terraform show -no-color "$planPath" | Out-File -FilePath "$planPath.txt" -Encoding utf8
          }
          finally {
              # 5. Cleanup: Remove the temporary Live Runner IP
              if ($null -ne $currentAgentIp) {
                  Write-Host "==> Cleanup: Removing Live Runner firewall rule: $currentAgentIp"
                  az keyvault network-rule remove --name "$env:KV_NAME" --ip-address "$currentAgentIp"
              }
              Pop-Location
          }
      env:
        TF_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)
        TF_STATE_RG: $(TFSTATE_RG)
        TF_STATE_SA: $(TFSTATE_SA)
        TF_STATE_CONTAINER: $(TFSTATE_CONTAINER)
        TF_STATE_KEY: $(TFSTATE_KEY)
        KV_NAME: $(KV_NAME)
        TF_VAR_AgentIP: $(TF_VAR_AgentIP)

    - publish: "$(Build.SourcesDirectory)/$(TF_ROOT)/tfplan"
      displayName: "Publish binary tfplan"
      artifact: tfplan

    - publish: "$(Build.SourcesDirectory)/$(TF_ROOT)/tfplan.txt"
      displayName: "Publish text tfplan"
      artifact: tfplan-text

    - publish: "$(Build.SourcesDirectory)/$(TF_ROOT)/.terraform.lock.hcl"
      displayName: "Publish terraform lockfile"
      artifact: tf-lockfile